// This is the header file included by an assembly file
// Only "define" statements are valid. No function prototypes, variables,
// or typedefs, because that are C concepts, not assembly ones.

#define TWO 2
#define THREE 3
#define ADD_TO_R0(x) add r0, r0, x
