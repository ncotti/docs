# CMake example

This is the command cheatsheet:

```bash
mkdir build
cd build
cmake ..
cmake --build .
cmake --install . --prefix $(pwd)/../install
../install/bin/tuto
```

Refer to the CMake documentation for details.
